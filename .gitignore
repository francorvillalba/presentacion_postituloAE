# Dependencias de Node (¡CRÍTICO IGNORAR ESTO!)
node_modules/

# Directorios de compilación (producción)
dist/
build/

# Archivos de variables de entorno (por si manejas claves o URLs privadas)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos de registro de errores
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos basura del sistema operativo
.DS_Store
Thumbs.db

#otros: 
openspec/
requerimientos.md
.atl/
